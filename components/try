import { cn } from "@/lib/utils";
import Marquee from "@/components/magicui/marquee";

const text = [
  {
    body: "I've never seen anything like this before. It's am love it.",
  },
  {
    body: "I've never seen anything lIt's amazing. I love it.",
  },
  {
    body: "I've never seen anything . It's amazing. I love it.",
  },
  {
    body: "I've never g like this before. It's amazing. I love it.",
  },
  {
    body: "I've never seen anything like this before. Iazing. I love it.",
  },
  {
    body: "I've never sthing like tIt's amazing. I love it.",
  },
];

const firstRow = text.slice(0, text.length / 2);

const ReviewCard = ({
  body,
}: {
  body: string;
}) => {
  return (
    <figure
      className=
        "relative h-fit w-fit cursor-pointer flex-nowrap overflow-hidden border"
    >
      <blockquote className="font-semibold text-xl">{body}</blockquote>
    </figure>
  );
};

const VerticalText = () => {
  return (
    <div className="relative flex flex-col flex-nowrap items-center justify-center overflow-hidden border ">
      <Marquee pauseOnHover className="[--duration:20s]">
        {firstRow.map((text:any) => (
          <ReviewCard key={text.body} {...text} />
        ))}
      </Marquee>
    </div>
  );
};

export default VerticalText;
